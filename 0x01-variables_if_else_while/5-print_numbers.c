#include <stdio.h>

/**
 * main - the beginning of the code
 *
 * Return: 0 on code success
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
